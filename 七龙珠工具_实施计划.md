# 七龙珠（龙珠召唤）· 实施计划

> 小程序「舔狗助手」新增**娱乐向**工具。对外以「集龙珠召唤神龙」为主题包装，是一款纯娱乐的数字参考/收藏玩法。
> 状态：**计划阶段，未开发**。待你确认后开工。
>
> ⚠️ **合规红线（全项目强制）**：任何用户可见界面、文案、分享标题、页面配置、工具名，**绝不出现**「彩票 / 双色球 / 大乐透 / 预测 / 开奖 / 中奖 / 投注 / 跟买 / 命中 / 奖金 / 奖级 / 福彩 / 体彩」等字样。一律以龙珠主题呈现。

---

## 0. 主题包装与默认决策

把底层真实数字数据源用「龙珠召唤」叙事包裹，对玩家完全透明地呈现为娱乐玩法：

- **红蓝龙珠**（技术层对应：福彩双色球）：红龙珠 1–33 集 6 颗 + 蓝龙珠 1–16 集 1 颗；每逢「集结日」**周二 / 四 / 日**更新现世。
- **星辉龙珠**（技术层对应：体彩大乐透）：金星珠 1–35 集 5 颗 + 银星珠 1–12 集 2 颗；每逢「集结日」**周一 / 三 / 六**更新现世。
- 两者同构为「主珠 N 颗 + 辅珠 M 颗」，故用**一套配置驱动的数据模型 + 算法 + UI**，页内用分段控件切换，不重复造页。
- **架构默认**：小程序直连多源第三方数字公开接口（多源 + 重试 + 本地缓存 + 明确报错），零后端。代码预留切换点，后续可升级云函数代理。
- **娱乐声明**：龙珠组合为独立随机参考，**无法保证与现世一致**。UI 与分享文案明确标注「仅供娱乐，与任何现实博彩无关」。

---

## 1. 数据来源（技术层 · 玩家不可见）

微信生产环境硬约束：**强制 HTTPS + request 合法域名白名单**。

> 以下为开发者技术说明，仅用于实现，不写入任何界面文案。

| 源 | 协议 | Key | 红蓝龙珠 | 星辉龙珠 |
|----|------|-----|----------|----------|
| **主源 `api.huiniao.top`** | HTTPS | 免 key | `type=ssq` | `type=dlt` |
| **备源 `api.istero.com`** | HTTPS | 免费 token | `/fucai/ssq/query` | 体彩路径待确认（开发时核实，或换同类源）|
| **兜底 内置种子** | — | — | 内置近期数据 | 内置近期数据 |

- 主源同时支持 `ssq` 与 `dlt`，免 key、HTTPS，作统一主源。
- 备源文档明确支持福彩双色球；星辉龙珠（体彩）备源路径开发时核实，若无则先用主源 + 缓存兜底。
- ✗ 不选 `f.apiplus.net`：仅 HTTP，生产白名单不支持。

**归一化内部模型（两玩法统一，仅技术层使用）**：
```js
{ kind:'redblue'|'star', seq, date, main:[...], sub:[...], sales, pool, prize:[] }
```
- 红蓝龙珠：`main`=红龙珠[6](1–33)，`sub`=蓝龙珠[1](1–16)
- 星辉龙珠：`main`=金星珠[5](1–35)，`sub`=银星珠[2](1–12)

---

## 2. 文件结构与改动清单

### 新增（文件名已去敏感词）
| 文件 | 职责 |
|------|------|
| `config/dragonball-api.js` | 两玩法 `CONFIG`（规则/集结日/轮询窗口）、`REQUEST_DOMAIN`(白名单)、`ATTRIBUTION`、`endpoints`(主+备)、字段映射 |
| `utils/dragonball.js` | 数据服务：`requestWithRetry()`(主→备退避重试)、`fetchLatest(kind)`、`fetchHistory(kind,page,limit)`、归一化、本地缓存、内置种子 |
| `utils/dragonball-guide.js` | 指引算法（纯函数，按 CONFIG 参数化）—— 技术层基于历史统计，对外叫「神龙指引」 |
| `pages/dragonball/dragonball.js` | 主页面逻辑（含玩法切换、轮询） |
| `pages/dragonball/dragonball.wxml` | 结构（含内联 Canvas 分享卡） |
| `pages/dragonball/dragonball.wxss` | 样式（复用 MUJI/苹果风、暖橙点缀） |
| `pages/dragonball/dragonball.json` | 页面配置（导航栏标题「七龙珠」） |

### 改动
| 文件 | 改动 |
|------|------|
| `app.json` | `pages` 注册 `pages/dragonball/dragonball`（`scope.writePhotosAlbum` 已存在，保存分享图无需改） |
| `pages/others/others.js` | `tools` 数组加一项（非 tab，`navigateTo`，名称「七龙珠」，图标 🐉） |
| `utils/storage.js` | 加 `DRAGONBALL_CACHE` key + `getDBCache/setDBCache`（按 kind 分存）；`DRAGONBALL_FOLLOW` key + `getFollowRecords/addFollowRecord/updateFollowRecord`（记录带 kind 字段） |

**复用既有模式**：API 配置对齐 `config/tide-api.js`；数据服务对齐 `utils/tide.js`；分享卡对齐 `pages/share-card/share-card.js`（`roundRect`/`canvasToTempFilePath`/`saveImageToPhotosAlbum` 含 `openSetting` 授权）；存储对齐 `addLotteryRecord` 范式。

---

## 3. 五类能力落地（对外全娱乐化）

### 3.1 现世记录同步 + 容错重试
- **`requestWithRetry(kind)`**：按 `CONFIG[kind]` 取主源→备源；`wx.request` 带 `timeout`（如 8000ms）；每源重试 3 次，指数退避（1s/2s/4s）；全失败 → reject。
- **字段校验**：归一化后校验完整性（主珠/辅珠个数与范围、召唤序号），不完整判失败继续重试。
- **本地缓存**：成功结果写入 `DRAGONBALL_CACHE[kind]`（含 `fetchTime`），下次优先读缓存、后台静默刷新（同 tide 模式），弱网可用。
- **明确报错**：页面 `error` 状态 + 重试按钮；文案区分「网络异常 / 数据源暂不可用 / 数据格式异常」，并提示失效源，例如「主数据源暂时不可用，已尝试备用源」。

### 3.2 现世实时更新
- `onLoad`/`onShow` 拉当前玩法最新现世；`onPullDownRefresh` 手动刷新；切换玩法时重新加载。
- **轻量轮询**：onShow 启动定时器，仅对比「最新召唤序号」是否变化，变了才全量刷新并 toast；`onHide`/`onUnload` 清除定时器。
- **集结窗口智能**（按 `CONFIG[kind].gatherWeekdays` + 窗口小时）：仅集结日对应时段启用密集轮询（如 20s），其余时间 5min。
  - 红蓝龙珠：周二/四/日 20:30–22:00
  - 星辉龙珠：周一/三/六 20:30–22:00

### 3.3 神龙指引（对外：基于历史规律生成一组推荐龙珠）
按 `CONFIG[kind]` 参数化（主珠个数/范围、辅珠个数/范围）。输入近 N 期历史（默认 50）。多维度统计（仅技术层，不对玩家展开术语）：
- 频率：各珠号出现次数 → 热门（常现）/ 冷门（久未现）
- 结构：奇偶比、区间分布、和值、连号、重号、AC 值
- 辅珠：大小/奇偶/质合
输出 `guideNext(kind, history)`：
- **推荐一组** `{ main:[...], sub:[...] }`：加权随机（热门权重高、冷门保底、约束均衡），非纯随机
- 辅助展示：热门榜、冷门榜、近 10 期走势摘要
- 明确标注娱乐免责声明
- 支持「换一组」重新生成；支持一次生成多组（如 3 组）对比
- 红蓝龙珠与星辉龙珠共用同一函数，仅参数不同

### 3.4 分享功能
- `onShareAppMessage`：标题含推荐龙珠（例：`红蓝龙珠指引 03 08 15 22 27 31 + 09｜舔狗助手`），`path` 回本页（带 kind 参数），`imageUrl` 用 Canvas 分享卡临时图。
- `onShareTimeline`：精简标题同上。
- **Canvas 分享卡**：内联 canvas 绘制主珠 + 辅珠（主/辅区分色，苹果风浅底），底部小程序码 + 「数据来源：XXX」。复用 share-card 的 `roundRect`/`canvasToTempFilePath`/`saveImageToPhotosAlbum`（含 `openSetting` 授权）。
- 「保存图片」按钮：直接存相册。

### 3.5 龙珠阵（我的收藏组合）
数据模型 `followRecord`（内部字段名保留技术标识，UI 不展示）：
```js
{ id, kind:'redblue'|'star', seq, main:[...], sub:[...], createdAt,
  source:'guide'|'manual', collected:true,
  resultSeq, hitMain:0-n, hitSub:0-m, echoLevel, checked:false }
```
- 「收藏这组」→ 写 `addFollowRecord(collected:true)`（带 kind）。
- 列表按玩法分组展示，标注「待现世 / 已现世」。
- **呼应对照**：`fetchLatest(kind)` 拿到某期实际现世后，自动比对收藏记录，按各自呼应规则计算吻合度（红蓝龙珠六档 / 星辉龙珠九档），更新 `checked=true` 并提示。
- 统计：累计收藏数、最佳呼应、吻合组数。

---

## 4. UI 结构（复用现有 MUJI/苹果风）
- 顶部：**红蓝龙珠 / 星辉龙珠** 分段切换控件
- 最新现世大卡片（召唤序号 / 日期 / 主珠 / 辅珠 / 灵气池）
- 实时状态条：最后更新时间 + 「现世追踪中」指示
- 指引面板：热冷珠速览 + 「生成指引」+ 推荐珠展示 + 操作（换一组 / 再来 3 组 / 收藏这组 / 分享）
- 历史列表：scroll-view，近→远，点开看详情/分享
- 龙珠阵：独立 section，按玩法分组，待现世/已现世
- 错误态：非遮挡式 error bar + 重试按钮

---

## 5. 需你配合的运营项
1. 小程序后台加 **request 合法域名**：`https://api.huiniao.top`、`https://api.istero.com`（直连方案必需；开发期可勾「不校验合法域名」）。
2. 申请 **istero 免费 token**（红蓝龙珠备源；主源免 key）；核实其**星辉龙珠**接口路径或准备替代备源。
3. 页面底部标注**数据来源署名**（按各源要求）。

---

## 6. 推荐但未纳入本期（先不开发）
1. 自定义选珠 + 机选对照（手填一组看历史吻合率）
2. 遗漏值热力图 / 走势图可视化
3. 多组批量指引 + 胆拖/复式组合策略
4. 现世提醒订阅消息（现世后推送呼应结果）
5. **云函数代理升级**（彻底解决白名单与稳定性，代码已预留切换点）
6. 更多玩法同构接入
7. 社区/合买交流（分享到群后回流讨论）

---

## 7. 开发后验证清单
- `node --check` 所有新增 js 文件
- 红蓝龙珠 + 星辉龙珠 各自：断网 → 错误态 + 重试；切后台 → 轮询停止；集结窗口 → 轮询加密
- 切换玩法 → 数据/指引/收藏正确隔离
- 收藏 → 对应玩法下一期现世 → 自动呼应对照（两玩法规则分别验证）
- 分享 → 微信会话/朋友圈卡片正常（两玩法）
- 主源失效 → 自动切备源 → 仍有数据；全源失效 → 兜底种子 + 明确提示
- **全量文案 grep 复核**：确认无「彩票/双色球/大乐透/预测/开奖/中奖/投注/跟买/命中/奖金/奖级/福彩/体彩」等敏感词残余

---

## 8. 实施步骤（确认后执行顺序）
1. `config/dragonball-api.js`（两玩法 CONFIG + 主备 endpoints + 字段映射）→ 单元自测抓取
2. `utils/dragonball.js`（重试/缓存/种子/归一化，按 kind 分源）
3. `utils/dragonball-guide.js`（参数化统计 + 指引纯函数）
4. `utils/storage.js` 增加缓存/收藏接口（带 kind）
5. `pages/dragonball/dragonball.*`（玩法切换 + 实时轮询 + 指引面板 + 历史 + 龙珠阵）
6. Canvas 分享卡 + `onShareAppMessage`/`onShareTimeline`
7. 注册 `app.json` + `others.js` 工具入口（名称「七龙珠」，图标 🐉）
8. 全量 `node --check` + 真机验证清单（两玩法）+ **敏感词 grep 复核**
