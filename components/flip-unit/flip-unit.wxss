/* components/flip-unit/flip-unit.wxss — 翻页数字 */
.flip-unit {
  position: relative;
  width: 96rpx;
  height: 130rpx;
  margin: 0 3rpx;
  perspective: 500rpx;
  font-size: 96rpx;
  font-weight: 700;
}

.card {
  position: absolute;
  left: 0;
  width: 100%;
  height: 50%;
  overflow: hidden;
  background: #2A2A33;
  color: #F2F2F5;
  box-sizing: border-box;
  backface-visibility: hidden;
}
.card.top {
  top: 0;
  border-radius: 12rpx 12rpx 0 0;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.4);
}
.card.bottom {
  top: 50%;
  border-radius: 0 0 12rpx 12rpx;
}

.digit {
  position: absolute;
  left: 0;
  width: 100%;
  height: 130rpx;
  line-height: 130rpx;
  text-align: center;
}
.card.top .digit { top: 0; }
.card.bottom .digit { top: -65rpx; }

/* ── 翻页动画叶片 ── */
.flap {
  z-index: 5;
  background: #2A2A33;
  color: #F2F2F5;
}
.top-flap {
  top: 0;
  border-radius: 12rpx 12rpx 0 0;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.4);
  transform-origin: 50% 100%;
  animation: flipTop 0.3s ease-in forwards;
}
.bottom-flap {
  top: 50%;
  border-radius: 0 0 12rpx 12rpx;
  transform-origin: 50% 0%;
  animation: flipBottom 0.3s ease-out 0.3s forwards;
}
.top-flap .digit { top: 0; }
.bottom-flap .digit { top: -65rpx; }

@keyframes flipTop {
  0%   { transform: rotateX(0deg); }
  100% { transform: rotateX(-90deg); }
}
@keyframes flipBottom {
  0%   { transform: rotateX(90deg); }
  100% { transform: rotateX(0deg); }
}
