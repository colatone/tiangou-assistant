# 七龙珠（龙珠召唤）· 上线前非代码清单

> 用途：代码已开发完成（`config/dragonball-api.js` / `utils/dragonball.js` / `utils/dragonball-guide.js` / `pages/dragonball/*`），本清单是**你（非代码）必须在微信后台 + 真机/模拟器上完成的动作**。按顺序做即可。

---

## 〇、总览（3 件大事）

| # | 事项 | 是否阻塞上传 | 是否阻塞正常使用 |
|---|------|--------------|------------------|
| A | 小程序后台加 request 合法域名 × 2 | ✅ 阻塞上传 | — |
| B | 申请 istero 免费 token（备源） | ❌ 不阻塞（无 token 自动降级到主源+种子） | ❌ 不阻塞 |
| C | 真机/模拟器验证（字段映射 + 分享卡） | ❌ 不阻塞 | ✅ 影响真实数据正确性 |

**最简发布路径**：A 做完即可上传发布（开发期用下方"不校验合法域名"开关先跑）；B、C 可在发布后补验。

---

## A. 配置 request 合法域名（必做，阻塞上传）

微信小程序**正式版**强制校验 request 合法域名，未配置会直接上传失败 / 线上请求被拦截。

### 要加的域名（照抄，含 https://）
```
https://api.huiniao.top
https://api.istero.com
```
> 这两个域名已在 `config/dragonball-api.js` 的 `REQUEST_DOMAIN` 中声明，与代码一一对应，不要加错。

### 后台操作步骤
1. 登录 **微信公众平台** https://mp.weixin.qq.com （用该小程序的**管理员/开发者**账号）
2. 左侧菜单 → **开发** → **开发管理** → **开发设置**
3. 找到「**服务器域名**」区 → 点「修改」（需管理员扫码）
4. 在 **request 合法域名** 框里逐行粘贴上面两个域名：
   - `https://api.huiniao.top`
   - `https://api.istero.com`
5. 点「保存」
6. ⚠️ 域名生效有**最长 10 分钟缓存**，保存后稍等再上传。

### 开发期免校验（先跑起来用）
微信**开发者工具**里，无需配域名也能调试：
- 顶部菜单「详情」→ 左侧「**本地设置**」→ 勾选：
  ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
- 勾上后，未配置后台域名也能真机/模拟器请求 api.huiniao.top。

### 接口路径速查（真机验证时对照用）
| 玩法 | 主源 URL（免 key） | 备源 URL（需 token） |
|------|--------------------|----------------------|
| 双Se（redblue / ssq） | `https://api.huiniao.top/interface/home/lotteryHistory?type=ssq&rows=30` | `https://api.istero.com/fucai/ssq/query?token=XXX` |
| 大Le（star / dlt） | `https://api.huiniao.top/interface/home/lotteryHistory?type=dlt&rows=30` | `https://api.istero.com/ticate/dlt/query?token=XXX` |

> 主源端点**已实测修正（2026-07-23）**：真实路径是 `/interface/home/lotteryHistory?type=ssq|dlt`（参数名 `type`，非 `code`）。返回 `{ code:1, data:{ last:{...}, data:{ list:[...] } } }`，号码字段为 `one~seven` 补零串。频繁请求会返回 `code:401` 限流（代码已识别且不重试）。

---

## B. 申请 istero 备源 token（可选，不阻塞）

- 备源 `api.istero.com` 免费但需 token。无 token 时：主源正常则直接用主源；主源失败则降级到**内置种子兜底**，功能不崩，只是无实时数据。
- 申请方式：去 `https://api.istero.com` 官网注册 → 申请免费 token → 拿到后把 token 传入 `utils/dragonball.js` 的 `fetchLatest(kind, token)` / `fetchHistory(kind, limit, token)`。
- **当前代码里 token 是外部传入参数，未硬编码**。若要用备源，需在该小程序的某处（如 `app.js` globalData 或后端下发）提供 token，再传给数据服务。建议：先不接，线上用主源 + 种子即可；后续要增强稳定性再做。

---

## C. 真机 / 模拟器验证清单（建议发布前跑一遍）

在微信开发者工具（勾了"不校验合法域名"）或真机预览里，逐项确认。

### C1. 主/备源字段映射（最关键）
`utils/dragonball.js` 的 `normalize()` 已做**自适应字段匹配**，识别以下写法：

- **期号 seq**：`expect` / `issue` / `code` / `qihao` / `id`
- **日期 date**：`time` / `date` / `openTime` / `datetime`（取空格前日期部分）
- **开奖号码 openCode**：`openCode` / `code` / `number` / `opencode`
- **号码拆分规则**：以 `+` 分割前区/后区，前区/后区内部以空格、逗号、中文逗号分隔后转数字

**验证做法**：
1. 打开七龙珠页面，看「最新现世」是否显示真实近期期号与号码（非种子那几条 2026-07-xx）。
2. 在工具里临时加一行日志（发布前删掉）：
   ```js
   dragonball.fetchLatest('redblue').then(function (it) {
     console.log('[verify-ssq]', JSON.stringify(it))
   })
   ```
3. 对照打印结果：
   - `seq` 应为真实期号（如 `2026-083`）→ 说明 `expect/issue` 命中
   - `main` / `sub` 应为数字数组且数量正确（双Se：6+1；大Le：5+2）→ 说明 `openCode` 拆分正确
   - `date` 为 `YYYY-MM-DD`
4. 若 `main` 为空或号码数不对 → 说明该源返回结构不在上述字段内，需**微调 `normalize`**（或换源）。把真实返回 JSON 贴给我，我改映射。

> ⚠️ **huiniao 实际返回字段以真机为准**（计划阶段未能实测）。这是上线前唯一需要你实跑确认的点。若 huiniao 返回的是数组外层不同结构，`extractList()` 也已兼容 `res.data[]` / `res.result[]` / `res.data` 对象 / 裸数组 四种。

### C2. 断网 / 主源失效 → 种子兜底
1. 开发者工具 → Network → 勾「Offline」（模拟断网）。
2. 刷新七龙珠页：应显示**种子数据**（`seq` 为 `2026-07x` 那几条，底部署名仍在），页面**不白屏、不报错弹窗**。
3. 控制台应看到：`[dragonball] 主备源均失败，使用种子兜底`（黄色警告，正常）。
4. 恢复正常网络 → 刷新 → 回到真实数据。

### C3. 玩法切换隔离
1. 在「双Se」下刷新，再切「大Le」：两者的「最新现世 / 现世编年 / 龙珠阵」应**各自独立**，缓存 key 按 kind 分隔（`redblue` / `star`）。
2. 确认切换后不会出现双Se 显示大Le 数据的情况。

### C4. 收藏 → 下一期现世呼应对照
1. 在「双Se」点「收藏这组」→ 龙珠阵出现一条「待现世」。
2. 等该玩法下一期真实现世后（或手动改本地缓存模拟 newer seq）→ 重新进入页面：该条应变为「已现世」并显示**呼应度 %**。
3. 确认呼应算法 `utils/dragonball-guide.js` 的 `compareWith()` 输出 0–100 的整数百分比。

### C5. 分享卡保存（权限）
1. 点「保存图片」→ 真机应弹出**保存到相册授权**。
2. 首次允许 → 相册出现龙珠分享卡（分色球 + 小程序码）。
3. 若之前拒绝过 → 应弹出「去设置」引导（`wx.openSetting`），点「去设置」能跳到授权页。
4. 点胶囊「···」→「转发」→ 走 `onShareAppMessage`；朋友圈走 `onShareTimeline`。

### C6. UI 合规自查（人工过审）
- 全文无「彩票 / 双色球 / 大乐透 / 预测 / 开奖 / 中奖 / 投注 / 跟买 / 命中 / 奖金 / 奖级 / 福彩 / 体彩」字样（已 grep 零残留）。
- ⚠️ **「双Se / 大Le」是双色球 / 大乐透的拼音缩写**，机器查不出、人工可能识别。若担心过审，建议改用更中性纯龙珠名（如「赤焰龙珠 / 星陨龙珠」），随时可改。
- 页面常驻娱乐免责声明「与任何现实博彩无关」已存在。

---

## D. 发布流程总清单（照做）

1. ☐ A：微信公众平台加 `https://api.huiniao.top` + `https://api.istero.com` 到 request 合法域名
2. ☐ 微信开发者工具：详情 → 本地设置 → 勾「不校验合法域名」（开发期）
3. ☐ C1：真机打印 `fetchLatest('redblue'/'star')` 确认字段映射正确（最关键）
4. ☐ C2–C5：断网兜底 / 玩法隔离 / 收藏呼应 / 分享卡保存 各跑一遍
5. ☐ 若字段不对 → 把真实返回 JSON 贴回，我修 `normalize`/`extractList`
6. ☐ 删除验证时临时加的 `console.log`
7. ☐ 微信开发者工具 → 上传 → 填版本号+备注 → 提交审核
8. ☐ （可选）B：申请 istero token 增强备源稳定性
9. ☐ 审核通过后手动发布

---

## 附：已知限制（非阻断，知悉即可）
- 主源 `huiniao` 为第三方免费接口，稳定性依赖对方；失败自动降级主源重试(3次)→备源(2次)→种子兜底。
- 种子数据为**结构正确的占位数据**，非真实开奖，仅在全源失败时兜底，界面无"实时"误导。
- 分享卡小程序码为静态图（指向首页），带参码需后端 `getwxacodeunlimit`，属后续增强。
- 「双Se / 大Le」缩写名的过审风险见 C6。
