# 赛博木鱼 · 发布前审核报告（V29.x）

审核时间：2026-07-20　|　视角：**产品经理 + 真实用户**　|　结论：**无阻断性 Bug，建议发布**

---

## 一、产品经理视角（功能完整度 / 一致性 / 风险）

### ✅ 已具备的核心能力
| 模块 | 状态 | 说明 |
|------|------|------|
| 敲击木鱼 | ✅ | 元宝造型 + 木棒三段式敲击动画（举高→猛砸→回弹）+ 音效（WebAudio/兼容降级）|
| +1 飘字 | ✅ | DOM 放射扇形，从鱼顶往上飘、落到运势横幅区域停住淡出 |
| 自动模式 | ✅ | 3 档速度（慢/中/快），开启后 3s 自动收起滑杆，离开页面自动停止 |
| 白噪音伴侣 | ✅ | 雨/风两种合成音 + 3 档音量，调完 1.5s 自动收起 |
| 木鱼棒显隐 | ✅ | 可开关，设置持久化 |
| 每日一签 | ✅ | 敲满 30 下解锁，按天缓存同一签文，常驻横幅 |
| 我的战绩（分享图）| ✅ | 640×1080 素雅暖杏卡，含真实小程序码，**直接存相册** |
| 历史战绩 | ✅ | 独立页面，近 30 天列表 + 活跃天数/总敲击汇总，近→远排序 |
| 图标 | ✅ | 全平台兼容 `💰`，工具中心入口一致 |

### ⚠️ 需知悉的非阻断项
1. **每日一签文案重复**：`utils/quote.js` 第 12 行与第 50 行均为"愿岁月可回首，且以深情共白头。"。因按天缓存，仅是两条索引指向同一句，无功能影响，可在后续版本去重。
2. **`wx.getSystemInfoSync` 已废弃**：`_doDrawShare` 仍在使用，当前基础库仍兼容，不阻断；建议后续替换为 `wx.getWindowInfo`。
3. **自动模式无强制时长上限**：开启后若不点停止会一直敲。但 `onHide`/`onUnload` 会停止，且去历史页/切后台都会停，符合预期，不构成风险。
4. **小程序码为静态图**：`/images/qrcode.jpg` 是固定码，指向小程序首页。若需带参数（如带分享人），需后端 `getwxacodeunlimit` 下发，属后续增强，非发布阻塞。

### 🔒 发布前已做的清理（死代码）
- 删除成就系统全部残留：`_checkAch` 注释行、`_reset`/`_showHistory`/成就系列函数、wxml 成就弹窗 DOM、wxss `.mask/.card/.ci/.ct/.cd/.cb/@keyframes cIn`、storage.js `MERIT_MILESTONES` 常量。
- 全量 `node --check` + JSON 解析校验：**全部通过**。
- 依赖确认：自定义 tabbar 组件、`images/qrcode.jpg` 均存在。

---

## 二、用户视角（上手体验 / 爽点 / 槽点）

### 😊 用起来爽的地方
- **一敲就有反馈**：木棒"咚"地砸下、波纹扩散、橙色 +1 扇形往上飞，三重视觉反馈很解压。
- **自动模式真省心**：点一下就能挂机攒财运，速度还能调，3 秒后工具箱自动收起，界面干净。
- **白噪音很加分**：配雨声/风声敲木鱼，氛围感拉满，音量三档够用。
- **分享图能直接存相册**：不用再点预览再保存，一步到位。
- **历史战绩独立页**：不被木鱼盖住，近 30 天一目了然，今天还有特殊标记。

### 🤔 用户可能的疑惑（建议答疑/后续优化）
- **"为什么敲了 30 下才出今日签？"** → 属预期设计（解压目标感），可在横幅提示已写明"再敲 X 下解锁"。
- **"自动敲会不会停不下来？"** → 不会，可随时点 ⏹ 停止，切走也会停。
- **"分享图的小程序码扫出来是首页？"** → 当前是固定码，后续可做带参数码。

### 🐞 未发现的可复现 Bug
- 自动滑杆不收起、小程序码遮挡文案、成就弹窗误触发、图标不显示 —— 历史反馈的问题**均已修复并验证**。

---

## 三、最终结论

> **可以发布。** 功能闭环完整，视觉与交互符合预期，历史反馈的 Bug 全部解决，死代码已清理，静态校验全过。唯一建议是上线后择机处理"签文重复"和"废弃 API"两个无害项。

### 发布检查清单（勾选）
- [x] 木鱼造型（元宝）+ 敲击动画
- [x] +1 飘字效果
- [x] 自动/手动敲击 + 速度档位
- [x] 白噪音 + 音量档位
- [x] 每日一签解锁逻辑
- [x] 我的战绩分享图 + 直接存相册 + 小程序码不遮挡
- [x] 历史战绩独立页面
- [x] 工具栏图标去色 / 木鱼棒开关
- [x] 死代码清理 + 全量静态校验
- [x] 自定义 tabbar 组件 + 小程序码资源就位
